---
import Layout from '../../layouts/Layout.astro';
import { Music } from '@lucide/astro';
import { getLangFromUrl, useTranslations } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title="Musician">
  
  <section class="max-w-5xl mx-auto px-6 flex-grow w-full flex items-center">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-start w-full min-w-0">
      
      <!-- Left Column: Huge Apple Slogan with Gradient -->
      <div class="space-y-4 min-w-0">
        <div class="inline-flex items-center gap-2 text-blue-500">
          <Music class="h-4 w-4" />
          <span class="text-2xs font-semibold uppercase tracking-widest">{t('musician.tag')}</span>
        </div>
        <h1 class="text-4xl sm:text-5xl lg:text-5xl xl:text-6xl font-bold tracking-tight leading-[1.05] text-transparent bg-clip-text bg-gradient-to-b from-white via-zinc-100 to-zinc-300 break-words" set:html={t('musician.title')}>
        </h1>
      </div>

      <!-- Right Column: Biography Content in Editorial style -->
      <div class="space-y-8 font-sans min-w-0 py-12">
        
        <div class="space-y-4">
          <h2 class="text-xs font-semibold uppercase tracking-wider text-blue-400">{t('musician.h1')}</h2>
          <div class="space-y-4 text-base text-[var(--color-text-secondary)] leading-relaxed text-justify">
            <p>
              {t('musician.p1')}
            </p>
            <p>
              {t('musician.p2_1')}<span class="text-white">{t('musician.p2_2')}</span>{t('musician.p2_3')}<span class="text-white">{t('musician.p2_4')}</span>{t('musician.p2_5')}
            </p>
          </div>
        </div>

        <div class="h-[1px] bg-[var(--color-border-apple)]/20"></div>

        <div class="space-y-4">
          <h2 class="text-xs font-semibold uppercase tracking-wider text-blue-400">{t('musician.h2')}</h2>
          <div class="space-y-4 text-base text-[var(--color-text-secondary)] leading-relaxed text-justify">
            <p>
              {t('musician.p3')}
            </p>
            <p>
              {t('musician.p4')}
            </p>
          </div>
        </div>

      </div>

    </div>
  </section>

</Layout>
